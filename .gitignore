# Vendor board SDK — 1.2 GB, unmodified reference material, never edited here
JC3248W535EN/

# Claude Code project-local directory
.claude/

# Arduino IDE build artifacts (*.cpp.tmp.XXXXX.hash files left in sketch folder)
LilFishTerminal/*.tmp.*
DeskTicker/*.tmp.*

# Compiled outputs
*.bin
*.elf
build/

# ...but DO publish the web-updater firmware binaries (ESP Web Tools needs them)
!docs/firmware/**/*.bin

# Private brand spec — kept locally, must NOT be committed/made public
DeskTicker_Brand_Kit.md
